# Backlog Plan — 2026-08-16

**Theme:** Codebase hygiene — resolve the remaining 500/401 error paths and dead code
left from the core delivery push.

## Candidates

| Item | Signal | Confidence | Action | Key |
|------|--------|-----------|--------|-----|
| Replace remaining `orElseThrow()` with 404 handling | codebase health | HIGH | created (proposal) | SCRUM-46 |
| Auth endpoints swallow exceptions → misleading 401 | codebase health | HIGH | created (proposal) | SCRUM-47 |
| Remove orphaned `car` training module | codebase health | HIGH | created (proposal) | SCRUM-48 |
| Implement error-reporting TODO in AccountActivatedListener | codebase health | HIGH | created (proposal) | SCRUM-49 |
| Lock balance read-modify-write (concurrency) | codebase health | HIGH | deferred | (noted below) |
| Catch-all error handler for unhandled paths | codebase health | HIGH | deferred | (noted below) |

## Suggestions (not yet ticketed)

- **Concurrency on balance RMW** — two parallel withdrawals can both pass the
  insufficient-funds check (noted by reviewers on SCRUM-19/20). HIGH value but needs a
  design decision on locking strategy (pessimistic vs optimistic); deliberately not
  auto-created to avoid an un-reasoned ticket. Deferred for a spike.
- **Catch-all error handler** — unhandled exceptions still return Spring's default 500
  body instead of the unified `ErrorResponse` (reviewer note on SCRUM-18). Blocked on
  deciding whether to expose generic vs detailed messages.

## Notes

- Dedup pre-check: all four created items were checked against open tickets — SCRUM-17
  covered only `findByUUID` paths, not these `orElseThrow` sites; the car module and
  auth 401 and listener TODO are unaddressed.
- All created tickets carry the `proposal` label. Promote via `/promote-batch` after
  review; nothing is `ai-ready` yet.
