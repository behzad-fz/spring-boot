# Backlog Plan — 2026-08-19

**Theme:** Error-handling hygiene and concurrency safety — close the remaining gaps in
the error contract, fix silent exception swallowing, and address the balance
read-modify-write race condition.

## Candidates

| Item | Signal | Confidence | Action | Key |
|------|--------|-----------|--------|-----|
| Balance read-modify-write has no locking (concurrent overdraft) | codebase health | HIGH | created (proposal) | BK-50 |
| Add catch-all error handler for unhandled exceptions | codebase health | HIGH | created (proposal) | BK-51 |
| CustomLocalDateDeserializer silently swallows parse exceptions | codebase health | HIGH | created (proposal) | BK-52 |
| UserController.findUserById exposes principal name in raw string | codebase health | HIGH | created (proposal) | BK-53 |

## Suggestions (not yet ticketed)

- None this run — all codebase-health findings were concrete enough to ticket directly.

## Notes

- **Roadmap is empty** — `docs/roadmap.md` has no near-term items populated. This batch
  is purely codebase-health driven. The human should populate the roadmap with product
  direction so the planner can generate roadmap-traced tickets in future runs.
- **No team feedback file** — `docs/ideas.md` does not exist. Consider creating one to
  capture "wouldn't it be nice" items from the human or reviewers.
- **No R&D signals** — no exploration or spike candidates identified this run.
- **Dedup pre-check**: all four items were checked against open non-Done tickets. The
  balance locking and catch-all handler were noted in the 2026-08-16 plan but deliberately
  deferred (not ticketed); the deserializer and UserController issues were not previously
  identified.
- All created tickets carry the `proposal` label. Promote via `/promote-batch` after
  review; nothing is `ai-ready` yet.
- **Board state**: BK-43 (backlog planner skill/command) is In Progress — this run
  IS the execution of that ticket. Previous plan's proposals (BK-46 through 49) remain
  in To Do awaiting promotion.
